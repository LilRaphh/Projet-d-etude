# ── Fine-tuning SmartWear Fashion LLM ────────────────────────────────────────
# À installer sur le PC fixe (RTX 5070 Ti, CUDA 12.8+, Python 3.10+)
#
# Installation :
#   pip install -r finetune/requirements.txt
#
# Unsloth — nécessite CUDA. Installe d'abord PyTorch séparément :
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#   pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
# ─────────────────────────────────────────────────────────────────────────────

# Core fine-tuning
unsloth>=2024.12
trl>=0.12.0
transformers>=4.46.0
peft>=0.13.0
accelerate>=1.0.0
bitsandbytes>=0.44.0

# Dataset
datasets>=3.0.0

# Utilitaires
sentencepiece>=0.2.0
protobuf>=4.25.0
huggingface-hub>=0.25.0

# Monitoring (optionnel — retire si non utilisé)
# wandb>=0.18.0
